rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // App uses custom phone-based auth — allow all access from client
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
